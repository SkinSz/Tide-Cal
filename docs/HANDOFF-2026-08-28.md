# HANDOFF — Post-Commit Session, 2026-08-28 (evening)

Audience: the next agent/session picking up Tide. This file is the single
source of truth for resuming; do not rely on chat history.

Read order: this file → docs/technical-debt.md → docs/contracts/ as needed.

════════════════════════════════════════════════════════════════════
1. CURRENT STATE
════════════════════════════════════════════════════════════════════

HEAD: 291db0d "Smoke-test fix round + sidecar port-crash fix + app icon"
Working tree: CLEAN (nothing uncommitted, nothing staged).
Previous base: cc8ad99 (DC-17 v2 backlogged, docs only).

What Tide does: local-first calendar app. Tauri 2 shell (Rust, WebKitGTK)
+ vanilla-TS frontend + Node sidecar (dist/sidecar.mjs) owning the
authoritative SQLite domain DB. Multi-device sync over TCP 47471 with
HLC clocks, change records, quarantine, conflicts, pairing (QR/stdio
codes), per-peer misbehavior ladder (DC-16), quarantine lifecycle with
resolution/retention (TD-005). App DB:
~/.local/share/com.tide.app/tide-domain.db (schema v5).

Key components:
- src/persistence/ — domain core, validator, sync engine, bridges
  (sidecar_server.ts = NDJSON stdio RPC dispatcher).
- src-tauri/src/lib.rs — Tauri commands; sync_op allow-list (~line 115)
  is a SILENT GATE: any new sidecar op MUST be added there or it fails
  with no failing test. src-tauri/src/sidecar.rs — child process mgmt.
- frontend/ — calendar.ts, sync_errors.ts, devices.ts, conflicts.ts,
  paired_devices.ts, dialog.ts, device-label.ts, store.ts (SECOND invoke
  path — two paths exist and have drifted before; keep them in sync).
- frontend/assets/ — transparent Tide icon set (32/64/128/256 PNG, ico,
  icns); also installed as the Tauri icon set in src-tauri/icons/.
- scripts/seed-quarantine.ts — QA seeder through the real validation
  path. MUST point TIDE_DB_PATH at the app DB. Run only while the GUI
  is closed (SQLite lock).
- tests/ — vitest; see §2 counts.

Build/verify commands:
  npx vitest run            # 33 files, 433 tests
  npx tsc --noEmit          # clean
  (cd src-tauri && cargo build)
  npm run ui:build && npm run sidecar:build   # then cargo build; cargo run
  # does NOT rebuild frontend/sidecar assets

Launch (fresh sessions MUST start this way):
  pkill -f "dist/sidecar.mjs"          # clear zombie sidecars first
  cd /home/skins/tide/src-tauri
  TIDE_SIDECAR_PATH=/home/skins/tide/dist/sidecar.mjs cargo run
(Vite dev server on :5173, if running, serves LIVE frontend; without it
the app loads dist/. The webview origin is observable at
~/.local/share/com.tide.app/localstorage/ — the filename proves it.)

════════════════════════════════════════════════════════════════════
2. VERIFIED STATUS (exact, at commit 291db0d)

  vitest: 33 files, 433/433 PASSED
  tsc --noEmit: clean   |   cargo build: Finished dev profile
  Runtime-verified (real GUI, driven via AT-SPI/accessibility + a
  persisted in-webview trace, instrumentation since reverted):
    - Fresh launch: Sync-Errors badge = 4 (correct; DB has 4 active,
      2 resolved seeded rows).
    - Dialog: Active (4) with real cards + Resolved (2); both
      list_quarantine and quarantine_stats work through the real
      app path (webview → invoke → Rust allow-list → sidecar → DB).
    - Instrumented badge trace proved: init/dialog-open/close refreshes
      all fire, stats IPC returns active:4 fresh (~9 ms), correct DOM
      element updated, no overwrite/race/cache. NO production fix was
      needed for the badge; the earlier "badge 0" report came from a
      stale-frontend session, not from shipped code.
  Diagnostic instrumentation: FULLY REVERTED before commit
  (grep-clean; only the word "diagnostics" in an original comment).

  Reporting rule (binding): every claim must state which layer verified
  it — (a) implemented in source, (b) present in built artifact,
  (c) verified by automated tests, (d) verified at runtime. Never claim
  UI work "done" from source inspection alone. (The pairing-code
  textarea HEIGHT adjustment was NEVER implemented — rows="4"
  unchanged; do not claim otherwise. The textarea appearing only after
  "generate code" is BY DESIGN.)

════════════════════════════════════════════════════════════════════
3. RECENTLY COMPLETED (all committed)

  - TD-001: quarantine sequence-gap resolution (impl + design proposal).
  - TD-002: non-finite-number validation.
  - TD-005: quarantine error lifecycle — resolution UI (Retry/Discard,
    two-step confirm), retention cap v5, reconcile-on-init, store.ts
    fallback-poisoning fix.
  - TD-006: two-tier peer misbehavior protection + Paired Devices view.
  - DC-16 approved (v2).
  - Recurrence surfacing (display-only; no design contract needed).
  - Frontend/Tauri invoke-path fix (devices.ts, @tauri-apps/api/core).
  - Pairing RPC serialization fix (async handleLine + regression test).
  - Sidecar EADDRINUSE crash fix: ensureListener() catches serveSync()
    rejection (logs, retries next call). APPROVED as its own change;
    implemented WITHOUT a separate blind verifier pass (owner-authorized
    on demonstrated root cause — noted in the commit message).
  - GUI smoke-test fixes: month-view dblclick, chip click UX contract
    (DOM-tested), Paired-button merge, native popup removal, card
    redesign, z-index fix.
  - App icon: transparent PNG/ico/icns set, wired into Tauri + favicons.

════════════════════════════════════════════════════════════════════
4. IMPORTANT LESSONS / PROCESS (binding)

  - Do NOT trust implementation summaries — check source, build, and
    runtime evidence. This session's runtime instrumentation proved a
    reported bug ("badge 0") did not exist in shipped code.
  - Substantial milestones require INDEPENDENT verification (blind
    subagent review: repo+specs only, no briefing on hypotheses).
  - The sync/persistence core gets hostile QA; ordinary UI changes do
    NOT automatically require a full protocol-level adversarial pass.
  - Always distinguish implemented / tested / build-verified /
    runtime-verified.
  - Vite HMR: during the diagnostic, the dev server served live source;
    final verification ran with instrumentation reverted and a full
    rebuild + test pass (433/433) — no stale-module risk in the commit.
  - Runtime-investigation toolkit that worked (keep for reuse):
      * Drive the GUI headlessly via pyatspi (AT-SPI): app name "app",
        frame "Tide", buttons found by name, doAction(0) to click.
      * Screenshots: `timeout 10 spectacle -b -n -o /tmp/x.png`.
      * cua-driver/computer_use sees NO Wayland windows — don't bother.
      * WebKit webview localStorage persists as UTF-16LE SQLite at
        ~/.local/share/com.tide.app/localstorage/ — good for ordered
        diagnostic traces that survive teardown.
      * WEBKIT_INSPECTOR_SERVER env var gives empty replies — skip.
      * Zombie sidecars hold 47471 and flake the port regression test —
        kill orphans before running tests.

════════════════════════════════════════════════════════════════════
5. OPEN WORK / DEBT

A. APPROVED, SHOULD BE IMPLEMENTED NEXT
  - (none queued — the smoke-fix round is committed and verified)

B. INVESTIGATIONS/DESIGN APPROVED, NOT YET IMPLEMENTED
  - Sidecar lifecycle/zombie hardening: a sidecar can outlive its GUI
    (systemd-user-parented orphan; Rust Drop/kill path in sidecar.rs
    ~107-112 does not always fire) and retain port 47471. The
    ensureListener() catch prevents a CRASH but inbound sync stays
    unavailable until the port frees. Owner approved investigating
    why Drop/kill does not terminate the child, plus a possible
    port-in-use pre-check surfaced to the GUI (proposal, needs design).
    DO NOT present the .catch() as a complete lifecycle solution.

C. NON-BLOCKING TECHNICAL DEBT
  - TD-001 remaining design debt: quarantine/revalidation behavior
    details (see docs/technical-debt.md).
  - TD-005 retention/history: retention cap shipped; long-term history
    considerations open.
  - Two frontend invoke paths (store.ts vs devices.ts) can drift —
    candidate for consolidation someday (not scheduled).
  - data/ dev DB now gitignored; safe to delete from disk anytime.

D. DEFERRED / FUTURE DESIGN CONTRACTS
  - DC-17 database encryption at rest: design contract BACKLOGGED
    (docs only, v2). SQLCipher AES-256, key in OS keystore per DC-05.
    NO production implementation requested or authorized.
  - Hardening QA campaign (owner-defined scope, NOT started): chaos/
    integration pass over cross-layer seams — multi-instance sync,
    restarts/crashes, repeated pairing, malformed RPC input, quarantine
    accumulation, snapshot+incremental mixing, convergence checks.
    Explicitly NOT in scope: cosmetic UI fuzzing.
  - .ics export (Outlook/Google/Apple) — wanted by owner; reminders +
    .ics need design contracts; CalDAV explicitly skipped.
  - SO_REUSEADDR / dynamic fallback port decision — needs owner
    decision; touches DC-11/DC-13 semantics.

════════════════════════════════════════════════════════════════════
6. NEXT AGENT — STARTING POINT

  1. First thing to inspect: this file, then docs/technical-debt.md,
     then `git log --oneline -15` and a clean `git status`.
  2. Relevant documents: docs/contracts/DC-15..DC-17 (as needed),
     docs/technical-debt.md (TD registry).
  3. Relevant tests: npx vitest run (433/33 expected green);
     tests/ensure_listener_port.test.ts and
     tests/pairing_stdio.test.ts guard this session's two critical
     fixes (kill orphan sidecars before the port test).
  4. Explicitly NOT authorized until the owner approves:
     - any DC-17 encryption implementation,
     - starting the hardening QA campaign,
     - sidecar lifecycle changes beyond investigation (the approved
       ensureListener() fix is committed; further lifecycle work is
       investigation/design first),
     - any new features or refactors not in a pasted work package.
  5. Do NOT re-investigate already-verified work: badge/invoke/
     allow-list/serialization fixes are committed AND runtime-verified
     (§2). Start from the documented state.
  6. Standing workflow: implementation → verification → independent
     blind review → owner approval BEFORE commit. Owner is an IT
     infrastructure admin (no dev jargon dumps; admin analogies).
     Decisions arrive as pasted Telegram work packages. Parallel cap:
     2-3 non-colliding workstreams.

— end of handoff —
