# HANDOFF — 2026-09-01, end of session (master @ 3535f0c, tree clean)

Read this before touching anything. It saves you a full reconstruction pass.

## What I did

Wrapped up the post-freeze recovery session. In commit order (oldest
first, all on master):

1. **`0e30f48` + `a527651` — DC-22 contract APPROVED** (Reminders &
   Local Notification Semantics, docs/contracts/). Owner decisions:
   D1 missed-reminder = show-on-launch with missed-marking, discard
   once the event itself has ended; D2 all-day events = PER-EVENT
   day-before reminder (tick on + pick time in the event dialog —
   NOT a global setting; the owner corrected my global-setting draft).
   Core invariant verbatim: "Notification delivery is an ephemeral
   local side effect and is not part of Tide's replicated logical
   state." Implementation NOT yet authorized.
2. **`1e628f7` — DC-21 contract APPROVED** (mDNS-to-sidecar endpoint
   plumbing, docs/contracts/). Written by lead after the drafting
   agent died empty (recurring failure pattern). Owner security
   amendment on D3, binding: live mDNS and last-known endpoints are
   ROUTING HINTS ONLY — only successful authenticated Noise_XX
   session establishment may confirm an endpoint's identity;
   persistence subject to D6 (write only after successful
   authenticated session). Implementation NOT yet authorized.
3. **`14758ef` — Standing report 2026-09-01 edition** +
   TD-012 (post-fix full-suite run) + TD-013 (harness EADDRINUSE
   flake) added to docs/technical-debt.md. Also corrected a stale
   claim: the DC-12 recurrence CREATION wave landed earlier
   (adbfe69/341e502/4d424c8) — the old standing report said it was
   missing.
4. **`43a17dc` — TD-014..019** recorded from two BLIND adversarial
   reviews I dispatched (sync/network + frontend/domain). Key
   findings: TD-014 (9/10) expandOccurrences hangs on
   DAILY/MONTHLY INTERVAL>1 and expands WEEKLY-without-BYDAY daily;
   TD-015 (8/10) dialog.ts derives override identity from the MOVED
   start → orphan overrides + duplicate chips; TD-016 (7/10)
   unchecking "Repeat" silently deletes the whole series (owner
   semantics decision PENDING — my recommendation: unchecking means
   "end recurrence here", never delete); TD-017 (6/10) c088023's
   CHANGES_ACK barrier deviates from frozen DC-08 §3.4; TD-018
   (6/10) dead-carrier session masquerades as clean success;
   TD-019 (3/10) sync hygiene (stash leak, FrameQueue single
   waiter, immortal pump, stale ACK frontier).
5. **`5863de8` — two SMALL review fixes applied by lead**: drag-ghost
   document listeners no longer accumulate per render (module-level
   handler + id guard); isRecurrenceConflictPair uses endsWith.
   Scoped suites green after (24/24).
6. **`3535f0c` — test infra + TD-020 (BLOCKING)**: the noise-c.wasm
   emscripten glue installs `process.on("unhandledRejection") →
   process.exit(1)` at evaluation, killing vitest workers and
   aborting whole runs (ERR_IPC_CHANNEL_CLOSED). Fixed at test level
   only: tests/setup-noise-c-guard.ts vetoes exit-handler
   re-registration (glue may evaluate lazily per worker — one-time
   removal is NOT enough), plus guardProcess() in the 8 probes that
   lacked it. NO production code touched.

## Verification (COMMAND → EXIT STATUS)

- `npx tsc --noEmit` → **0**
- `npx vitest run` (full suite, post-infra-fix, run twice) →
  **exit 1: 681 passed / 4 failed of 685** — all 4 failures are ONE
  deterministic root cause, recorded as TD-020:
  - tests/revocation_propagation.test.ts TRP-1 + TRP-4b: barrier
    exits on peer CHANGES_ACK before B's REVOCATIONS_ACK is
    processed → A's revocation queue never drains (got 1, want 0).
  - tests/pkg5b_snapshot_conflicts.test.ts + tests/full_state_triggers.test.ts:
    serve/barrier hangs (SyncIdleTimeoutError after 15000ms in
    serve/barrier) under in-memory transports that never receive
    close() — runSession never closes the transport, so a side that
    exits its barrier parks the peer on the idle bound.
  - The three-device harness (real TCP) still passes 7/7 — real
    transports propagate FIN, so the regression only shows in-process.
  - The 4 failures are NOT flaky: they fail deterministically,
    including file-alone runs.
- `node tests/probes/three_device_harness.mjs` → 7/7 PASS, 0 session
  errors (unchanged from c088023 verification).

## Issues encountered

- The 08-31 "638/638 green" baseline is stale in one direction and
  misleading in the other: c088023 (the sync-stall fix, reviewed
  SOUND by a blind reviewer on its own terms) regressed 4 tests that
  the harness gate could not see. Lesson (now institutionalized as
  TD-020's fix direction): harness green ≠ suite green; the barrier
  change needed in-memory-transport coverage.
- The full-suite run was twice deferred by the one-suite-at-a-time
  rule (blind reviewers holding the repo) — that deferral is why the
  regression surfaced hours later than it could have. TD-012 is now
  effectively superseded by TD-020 (the run happened; it found real
  failures; closure waits on the fix).
- Two delegated agents died with empty summaries ("Now") after doing
  real research — salvage their transcripts before re-dispatching.
  This is the third occurrence; briefs must carry a "never return an
  empty summary" clause and an explicit deliverable filename.

## Open items a successor should pick up (priority order)

1. **TD-020 (BLOCKING)** — one bounded sync package: amend the
   barrier (bounded post-ACK drain so sibling acks like
   REVOCATIONS_ACK are processed before exit; consider DC-08 v2
   amendment formalizing barrier semantics per TD-017) + transport
   close() semantics so in-memory AND real transports EOF
   deterministically at session end. Tests FIRST: the 4 failing
   tests are the regression proof (they fail now, must pass after).
   No timeout changes (agent rule 2). Reviewer evidence:
   subagent-summary-0-20260901_012624_365151.txt (F1/F2 map to
   TD-018/TD-017 — fold them into the same package).
2. **Recurrence wave** — TD-014 + TD-015 (+ TD-016 after the owner
   picks semantics). Reviewer evidence:
   subagent-summary-0-20260901_012847_247593.txt (F1-F3 = TD-014,
   F4 = TD-015, F5 = TD-016). Its "did NOT pan out" list saves you
   re-attacking clean areas (timezone/DST, XSS, click-swallowing).
3. **DC-21 / DC-22 implementations** — both contracts APPROVED, both
   awaiting the owner's explicit go. Do NOT start them unprompted.
4. **TD-011 owner visual pass** — release gate, now includes the
   recurrence wave.

## Standing rules that bit this session (do not re-learn them)

- ONE vitest suite per repo at a time; bounded workers (14 GB host —
  two agents' concurrent fleets caused a kernel-OOM freeze + reboot
  on 09-01 ~00:35; postmortem c13e457).
- NEVER ratchet test timeouts to get green — report and stop.
- Sibling-repo/ports contention = STOP and report, not continue.
- Full-suite verification after any protocol-layer change: scoped
  suites + the real-TCP harness can both stay green while 4 in-proc
  tests regress. TD-020 is the proof.
